<html>
	<head>
		<meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
	</head>
	<body>
		<h1>Novo Comentário</h1>
		<!-- tem que pegar o link do profile do profissional, serviço e usuário que comprou, mudar quando der build -->
		<p><a href="<%= @base_url %>profile/<%= @user.id %>"><%= @user.name %></a> acabou de comentar a atividade: <a href="<%= @base_url %>aventura/<%= @service.id %>"><%= @service.name %></a> de <a href="<%= @base_url %>profile/<%= @professional.id %>"><%= @professional.user.name %></a>.</p>
		<p>Comentário:</p>
		<p><%= @acquisition.commentary</p>
	</body>
</html>